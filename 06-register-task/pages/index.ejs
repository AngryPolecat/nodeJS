<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
    <title><%=title%></title>
  </head>
  <body>
    <%- include('./partials/guest-header') %>

    <section class="h-100">
      <div class="container py-5 h-100">
        <% if (error) {%>
        <div class="alert alert-danger"><%= error %></div>
        <% } %>
        <!---->
        <% if (create) {%>
        <div class="alert alert-success">Вы успешно записались к врачу</div>
        <% } %>
        <div class="row d-flex justify-content-center align-items-center h-100">
          <div class="col">
            <div class="card card-registration my-4">
              <form method="POST" action="/" class="card-body p-md-5 text-black">
                <h3 class="mb-5 text-uppercase">Запись к врачу</h3>

                <div class="form-outline mb-4">
                  <label class="form-label" for="fio">ФИО</label>
                  <input type="text" name="fio" id="fio" class="form-control form-control-lg" required />
                </div>

                <div class="form-outline mb-4">
                  <label class="form-label" for="phone">Номер телефона</label>
                  <input type="text" name="phone" id="phone" class="form-control form-control-lg" placeholder="+7(000)000-00-00" required />
                </div>

                <div class="form-outline mb-4">
                  <label class="form-label" for="problem">Опишите вашу проблему</label>
                  <textarea type="text" name="problem" id="problem" class="form-control form-control-lg"></textarea>
                </div>

                <div class="d-flex justify-content-end pt-3">
                  <button type="reset" class="btn btn-light btn-lg">Сбросить</button>
                  <button type="submit" class="btn btn-warning btn-lg ms-2">Отправить</button>
                </div>
              </form>
            </div>
          </div>
        </div>
      </div>
    </section>

    <script src="https://unpkg.com/imask"></script>
    <script src="/app.js"></script>
  </body>
</html>
