<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
    <title><%=title%></title>
  </head>
  <body>
    <%- include('./partials/user-header') %>
    <!-- \n -->
    <% if(error) { %>
    <div class="alert alert-danger"><%= error %></div>
    <% } %>

    <div class="container">
      <h2>Запись к врачу</h2>
      <% if(problems.length) { %>
      <div class="d-flex justify-content-between mb-1">
        <div>
          <input type="text" name="search" id="search" class="form-control form-control-lg" placeholder="Поиск..." />
        </div>
        <div class="d-flex justify-content-end">
          <button type="button" class="btn btn-light btn-lg">По дате</button>
          <button type="button" class="btn btn-light btn-lg ms-2">По ФИО</button>
          <button type="button" class="btn btn-light btn-lg ms-2">&#9668;</button>
          <button type="button" class="btn btn-light btn-lg ms-2">&#9658;</button>
        </div>
      </div>
      <div class="card">
        <ul class="list-group">
          <% for (let i = 0; i < problems.length; i++) {%>
          <li class="list-group-item d-flex justify-content-between align-items-center">
            <span class="w-25 d-inline-block"><%= problems[i].date %></span>
            <span class="w-25 d-inline-block"><%= problems[i].fio %></span>
            <span class="w-25 d-inline-block"><%= problems[i].phone %></span>
            <span class="w-25 d-inline-block"><%= problems[i].problem %></span>
          </li>
          <% } %>
        </ul>
      </div>
      <% } else { %>
      <div>No problems</div>
      <% } %>
    </div>
    <script src="/app.js"></script>
  </body>
</html>
